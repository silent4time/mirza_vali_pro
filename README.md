# mirza_vali Pro

ربات فروش و مدیریت اشتراک VPN برای **تلگرام** و **بله**

**نسخه:** 4.0.7

> **mirza_vali Pro** — مسیر نصب پیش‌فرض: `/home/mirza_vali_pro`


> **mirza_vali Pro** از نسخه **4.0.4** — شامل Connectix، چندنصب موازی و امکانات سفارشی Bale/Eylan.


---

## معرفی

`mirza_vali Pro` یک ربات کامل برای فروش سرویس VPN است که همزمان روی **تلگرام** و پیام‌رسان **بله** کار می‌کند. خرید، پرداخت، تحویل خودکار سرویس، کیف پول و پنل مدیریت ادمین در یک سیستم یکپارچه پیاده‌سازی شده است.

این پروژه بر پایه ربات متن‌باز **میرزا** توسعه یافته و برای استفاده عملی در فروش اشتراک سفارشی شده است.

### تشکر

با سپاس از سازنده ربات اصلی **میرزا**:  
[https://github.com/mahdiMGF2/mirzabot](https://github.com/mahdiMGF2/mirzabot)

---

## امکانات اصلی

| قابلیت | توضیح |
|--------|--------|
| تلگرام + بله | یک دیتابیس مشترک؛ پیام و وب‌هوک جدا برای هر پلتفرم |
| فروش خودکار | انتخاب محصول، پرداخت، ساخت سرویس و ارسال کانفیگ |
| کیف پول | شارژ موجودی و خرید از موجودی |
| پنل‌های متعدد | پشتیبانی از پنل‌های رایج مدیریت VPN |
| مدیریت ادمین | محصولات، کاربران، پرداخت‌ها، آمار، بک‌آپ |
| Apache + SSL | نصب روی Apache با گواهی SSL |
| منوی سرور | نصب، آپدیت، حذف، ریست و تمدید SSL با یک اسکریپت |


---

## با افزودن امکانات زیر

امکانات سفارشی اضافه‌شده نسبت به ربات پایه میرزا:

| امکان | توضیح |
|--------|--------|
| **تلگرام + بله یکپارچه** | وب‌هوک و پیام جدا؛ دیتابیس مشترک؛ گزارشات به هر دو گروه |
| **بله‌پی از تلگرام** | راهنمایی کاربر تلگرام به ربات بله برای پرداخت آنلاین |
| **کارت‌به‌کارت پایدار** | فاکتور، نمایش کارت، رسید و تأیید ادمین بدون گیر کردن در پرداخت ناقص |
| **پنل ایلان (Eylan)** | جداسازی پروتکل با `inboundid` (openvpn / wireguard / l2tp / cisco / multi) |
| **کاربر همزمان محصول** | هنگام ساخت محصول: تعداد کاربر همزمان؛ `0` = نامحدود؛ ارسال به API ایلان (`max_clients`) |
| **تعرفه اشتراک‌ها** | لیست محصولات با آیکون 🔋 حجم · ⏳ زمان · 👥 کاربر · 💰 قیمت؛ سورت بر اساس قیمت؛ پنل بولد |
| **بکاپ دستی** | از منوی گزارشات → بکاپ فوری SQL به گروه تلگرام و بله (یک فایل، بدون تکرار) |
| **زمان‌بندی بکاپ** | فاصله به ساعت + ساعت شروع (مثلاً از ۱۴ هر ۲ ساعت، یا از ۰۰ هر ۲۴ ساعت) |
| **منطقه زمانی** | انتخاب timezone از پنل ادمین (پیش‌فرض Asia/Tehran)؛ تاریخ شمسی با همان ساعت |
| **گروه گزارش بله** | تنظیم آیدی عددی گروه بله جدا از تلگرام |
| **مسیر نصب ثابت** | نصب در `/home/mirza_vali_pro` با منوی مدیریت نصب/آپدیت/حذف/SSL |
| **لایسنس اشتراکی (Pro)** | قفل دامنه؛ ۱–۱۲ ماه؛ هشدار ~۷ روز قبل و در انقضا؛ انقضا فقط فروش را متوقف می‌کند؛ مهلت ۹۶ ساعت اگر سرور لایسنس قطع باشد؛ API: `license.example.com` |

> جزئیات ایلان در بخش «پنل ایلان» همین فایل آمده است.




## لایسنس Pro

از نسخه **4.0.4** ربات پرو به سرور لایسنس شما وصل می‌شود.

| مورد | توضیح |
|------|--------|
| قفل | فقط روی **دامنه** ربات |
| مدت | ۱ تا ۱۲ ماه (از پنل لایسنس) |
| انقضا | فقط **فروش** متوقف می‌شود؛ خود ربات آنلاین می‌ماند |
| هشدار | حدود ۷ روز مانده + پیام انقضا برای ادمین |
| قطع سرور لایسنس | تا **۹۶ ساعت** با آخرین وضعیت ذخیره‌شده کار می‌کند |
| API | `https://license.example.com/api/verify.php` |
| پنل شما | `https://license.example.com/admin/` |

### تنظیم در config.php مشتری

```php
$LICENSE_ENABLED = true;
$LICENSE_KEY = 'MVP-...';
$LICENSE_API_URL = 'https://license.example.com/api/verify.php';
// $domainhosts باید همان دامنه قفل‌شده باشد
```

### تست ادمین

در ربات دستور زیر را بفرستید:

```text
/license
```


## روش‌های پرداخت

- **بله‌پی** — پرداخت آنلاین داخل بله؛ از تلگرام نیز با راهنمایی به ربات بله هدایت می‌شود
- **کارت‌به‌کارت** — نمایش شماره کارت، دریافت رسید و تأیید توسط ادمین
- **کیف پول ربات** — خرید مستقیم از موجودی کاربر

تنظیمات پرداخت از پنل مدیریت ربات (تلگرام / بله) انجام می‌شود و بین هر دو پلتفرم هماهنگ است.

---

---

## پنل‌های پشتیبانی‌شده

| نوع پنل | شناسه در ربات |
|---------|----------------|
| Marzban | `marzban` |
| Marzneshin | `marzneshin` |
| پاسارگاد (Pasarguard) | `pasarguard` |
| میرزا ایجنت | `mirza_agent` |
| سنایی (X-UI Single) | `x-ui_single` |
| علیرضا (Alireza Single) | `alireza_single` |
| Hiddify | `hiddify` |
| فروش دستی | `Manualsale` |
| WGDashboard | `WGDashboard` |
| s-UI | `s_ui` |
| IBSng | `ibsng` |
| MikroTik | `mikrotik` |
| Rebecca | `rebecca` |
| **Eylan Panel** | `eylan` |

افزودن پنل از منوی مدیریت ادمین ربات → مدیریت پنل‌ها → افزودن پنل.

---

## پنل ایلان (Eylan) — نحوه استفاده

پنل ایلان چند پروتکل را روی یک سرور پشتیبانی می‌کند (OpenVPN، WireGuard، L2TP/IPsec، Cisco AnyConnect و حالت ترکیبی Multi).

**نکته مهم:** در ربات، برای **هر نوع VPN باید یک پنل جداگانه** تعریف شود. نباید همه پروتکل‌ها را با یک ردیف پنل بفروشید.

### مراحل پیشنهادی

1. در پنل مدیریت ادمین، برای هر پروتکل یک پنل با نوع **EylanPanel** بسازید.
2. آدرس API و کلید API (همان فیلد رمز پنل) را وارد کنید.
3. در فیلد **Inbound / inboundid** یکی از مقادیر زیر را دقیقاً بنویسید:

| مقدار inboundid | خروجی برای کاربر |
|-----------------|------------------|
| `openvpn` | فایل‌های `.ovpn` + صفحه کاربری |
| `wireguard` | فایل‌های `.conf` (+ QR در صورت پشتیبانی) |
| `l2tp` | سرور، یوزرنیم، رمز، Shared Key (PSK) |
| `cisco` | سرور، یوزرنیم، رمز |
| `multi` | ترکیب پروتکل‌های فعال (OVPN + WG + L2TP + Cisco) |

4. برای هر پنل یک **محصول** جدا بسازید و لوکیشن/پنل همان ردیف را به محصول وصل کنید.
5. اگر روی سرور ایلان چند instance وایرگارد دارید، ربات کانفیگهای موجود را از API می‌گیرد و ارسال می‌کند.

### مثال نام‌گذاری

- پنل: `ایلان OpenVPN` → inboundid = `openvpn` → محصول OpenVPN  
- پنل: `ایلان WireGuard` → inboundid = `wireguard` → محصول WireGuard  
- پنل: `ایلان L2TP` → inboundid = `l2tp` → محصول L2TP  
- پنل: `ایلان Cisco` → inboundid = `cisco` → محصول Cisco  
- پنل: `ایلان Multi` → inboundid = `multi` → محصول Multi  

> اگر فقط یک پنل با inboundid اشتباه یا خالی بسازید، ممکن است همه پروتکل‌ها با هم ساخته شوند یا خروجی نامرتب باشد.


---

## مقایسه با میرزای اصلی و پنل ایلان (مبنای v2.0)

### میرزای متن‌باز رایگان ([mahdiMGF2/mirzabot](https://github.com/mahdiMGF2/mirzabot) ≈ 0.3.x)

| موضوع | میرزا رایگان 0.3 | mirza_vali |
|--------|------------------|------------|
| معماری | بازنویسی جدید (کلاس‌ها، API داخلی) | مبتنی بر نسل قبلی میرزا + سفارشی‌سازی |
| تلگرام | ✅ | ✅ |
| بله + بله‌پی | ❌ | ✅ |
| کارت‌به‌کارت / کیف پول | محدود/متفاوت | ✅ پایدار و تست‌شده |
| پنل ایلان | ❌ | ✅ کامل |
| بکاپ زمان‌بندی‌شده از ربات | محدود | ✅ دستی + بازه + ساعت شروع |
| منطقه زمانی | — | ✅ قابل انتخاب |
| تعرفه / کاربر همزمان | — | ✅ |

> **نکته مهم:** ادغام کامل کد 0.3 داخل mirza_vali بدون بازنویسی کل پروژه، **بک‌اند بله، پرداخت و ایلان را می‌شکند**. نسخه ۲ روی همان هستهٔ پایدار شماست و فقط قابلیت‌های ایمن از آپ‌استریم را لایه به لایه اضافه می‌کند.

نسخهٔ اشتراکی/پرو میرزا (کانال رسمی) امکانات بیشتری دارد (مثلاً پنل سفارشی API، pay-as-you-go، ایموجی پریمیوم) که در ریپوی رایگان عمومی نیست و قابل کپی قانونی از سورس باز نیست.

### پنل ایلان / OVPN Manager ([eylandoo](https://github.com/eylandoo/openvpn_webpanel_manager) ≈ 4.x)

| امکان ایلان 4.x | در mirza_vali |
|-----------------|---------------|
| OpenVPN / WG / L2TP / Cisco / Multi | ✅ از قبل |
| `max_clients` | ✅ |
| جداسازی با inboundid | ✅ |
| API revoke / toggle / reset_traffic | ✅ توابع آماده (v2) |
| HWID devices / reset | ✅ توابع آماده (v2) |
| list_all users | ✅ توابع آماده (v2) |
| Sing-box (VMess/VLESS/…) | ⏳ هنوز در ربات به‌عنوان محصول جدا پیاده نشده (نیاز طراحی محصول جدا) |
| Sub-admin / reseller API | ⏳ خارج از محدوده فروش کاربر نهایی فعلی |

مستندات API: [eylandoo.github.io](https://eylandoo.github.io)



### پنل Connectix (از v3.0)

- نوع پنل: **Connectix** در منوی افزودن پنل
- آدرس API: `https://seller-api.connectix.vip/external/v1`
- پسورد پنل = **API Token** (Account Settings → API)
- پس از ذخیره، پلن‌های فعال به‌صورت خودکار به‌عنوان محصول با دسته **کانکتیکس** ساخته می‌شوند
- همگام‌سازی مجدد: پیام ادمین `🔄 همگام پلن کانکتیکس`



### چند نصب موازی (Multi-instance) — از v3.0.1

روی **یک سرور** می‌توانید چند ربات جدا نصب کنید:

| باید جدا باشد | مثال |
|---------------|------|
| مسیر نصب | `/home/mirza_vali_pro` و `/home/mirza_vali2` |
| دامنه | `bot1.example.com` و `bot2.example.com` |
| دیتابیس | نام/یوزر جدا (پیش‌فرض بر اساس نام پوشه) |
| توکن تلگرام/بله | هر ربات توکن خودش |

- هر نمونه env جدا در `/etc/mirza_vali/instances/<id>.env` دارد.
- پورت آپاچی اختصاصی جدا برای هر نمونه انتخاب می‌شود.
- در منو: گزینه **۹) List instances** — و هنگام Update/Remove اگر چند نمونه باشد، از شما می‌پرسد کدام را انتخاب کنید.
- نصب نمونهٔ دوم: گزینه **۱** با مسیر جدید (مثلاً `/home/mirza_vali2`).


## مسیر نصب

| مورد | مسیر |
|------|------|
| پوشه ربات | **`/home/mirza_vali_pro`** |
| سورس منوی مدیریت | `/opt/mirza_vali-src` |
| فایل تنظیمات نصب | `/etc/mirza_vali/install.env` |

> پیش‌فرض نصب همیشه `/home/mirza_vali_pro` است.

---

## نصب

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/silent4time/mirza_vali_pro/main/install.sh)
```

از منو گزینه **1) Install** را انتخاب کنید.

در طول نصب دامنه ربات، توکن تلگرام، توکن بله و اطلاعات دیتابیس از شما پرسیده می‌شود.

---

## آپدیت

```bash
cd /opt/mirza_vali-src && bash manage.sh
```

گزینه **2) Update**

---

## منوی مدیریت

| گزینه | کار |
|-------|-----|
| 1 | نصب |
| 2 | آپدیت به آخرین نسخه |
| 3 | حذف کامل |
| 4 | ریست وب‌هوک / تونل / دامنه |
| 5 | تمدید گواهی SSL |
| 6 | راهن و پارامترها |
| 7 | وضعیت نصب |
| 0 | خروج |

---

## ساختار فایل‌های گیت‌هاب

در ریشه ریپو فقط این چهار فایل قرار می‌گیرد:

| فایل | نقش |
|------|-----|
| `install.sh` | نصب‌کننده یک‌خطی |
| `mirza_vali-latest.zip` | بسته کامل آخرین نسخه |
| `README.md` | همین مستند |
| `generate-installer.html` | تولید دستور نصب |

---

## حمایت مالی

اگر از پروژه استفاده می‌کنید و مایل به حمایت هستید:

**USDT / TON روی شبکه BEP20 (BSC):**

```text
0xa1a89602DDB47682091Af3Da1737672bd97f5669
```

---

## سلب مسئولیت

این نرم‌افزار برای مدیریت قانونی سرویس‌های خودتان است. مسئولیت پیکربندی سرور، دامنه، امنیت و رعایت قوانین محلی با بهره‌بردار است.

---

**ریپو:** [https://github.com/silent4time/mirza_vali](https://github.com/silent4time/mirza_vali)
